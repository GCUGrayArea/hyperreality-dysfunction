# OpenAI API Configuration (PR-011: Server-side only, no VITE_ prefix)
# Get your API key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Model configuration (defaults to gpt-4o-mini if not set)
OPENAI_MODEL=gpt-4o-mini
OPENAI_VISION_MODEL=gpt-4o-mini

# Note: These variables are used by Vercel Serverless Functions
# and are NOT exposed to the client-side code (no VITE_ prefix).
# For local development, use: vercel dev
